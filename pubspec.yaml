name: smart_financial_recovery_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  equatable: ^2.0.8
  firebase_core: ^4.5.0
  flutter:
    sdk: flutter
  flutter_pdfview: ^1.4.4
  google_sign_in: ^7.2.0
  provider: ^6.1.5+1
  firebase_auth: ^6.0.0
  go_router: ^17.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    # - assets/images
    - assets/icons/
